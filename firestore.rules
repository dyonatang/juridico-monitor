rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Todo acesso é feito pelo servidor (firebase-admin), que ignora estas regras.
    // Bloqueia qualquer acesso direto de clientes (web/mobile).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
